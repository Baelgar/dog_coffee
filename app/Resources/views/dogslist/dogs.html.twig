{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

        <div class="row">
            {% for dog in dog_profiles %}

                <div class="col-sm-6 col-md-4">
                    <a href="{{ path('chien') }}" class="thumbnail">
                        <img src="/images/{{ dog }}">
                    </a>
                </div>

            {% endfor %}
        </div>

    </div>

{% endblock body %}